# 3D 位置的再核查与可辨识性检验

目前可以计算受模型约束的 XYZ 估计，但不能用现有两个文件唯一确定真实 XYZ。
V12 的黄色轨迹改善了图像对齐，不能据此认定 3D 深度已解决。

## 发现并纠正的前提

`Path 2.csv` 给出 71 个中心线点，没有管腔表面。
`l1_size_cue.npz` 的宽度测量是在旧相机投影下取得的；有 142/761 个位置没有测到宽度。
V8、V11、V12 最初将缺测宽度用全局中位数填补，然后施加 3–6 mm 的半径限制。
**本审查之后已改为策略 C**（`report.md` V8b）：在当前相机投影上实测双侧管壁，
未测处取 3 mm 保守下限，不再用中位数填补。下限仍是建模假设，不是真实的 3D 血管边界。

按策略 C 重跑后，V12 的 70 帧 `geometry_conflict` 中，58 帧位于 3 mm 下限半径、
12 帧有当前投影的实测宽度（旧版 62 帧中 43 帧填补、19 帧有旧测量；冲突增加是因为
分叉处半径从填补的 5.3 mm 变为 3.0 mm 下限）。
因此，“70 帧与真实解剖冲突”是不成立的；正确说法是“70 帧与当前半径代理模型冲突”。

V13 已重新使用当前相机的投影，从背景图逐个采样橙色管腔两侧边界：
538/761 个位置可取得有效宽度；109–119 mm 的分叉区域仍无可靠的、包围所投影轴线的
双侧宽度。不能为了覆盖机器人而把这个区域的半径任意放大。V8b 验证了这一点的实际
影响：分叉处用 3 mm 下限而非填补值，V8 的增益不变（失败率 0.9%，留出 1.1%，分叉段
max 30.3 px）；但完全不给横向自由度则退回轴线模型的数字（3.4%）。

新导出的 `out/v13_3d_positions_with_provenance.csv` 保留 XYZ 代表估计，
同时输出半径来源和深度状态。133 个有效检测帧的半径来自 3 mm 下限（当前投影未测到
双侧管壁），标记 `radius_unmeasured_depth_unbounded`：这里的 unbounded 表示所给资料没有提供可靠边界，
不是说物理血管无限大。

## 为什么不能靠继续调参得到唯一真实深度

在已拟合的相机下，设相机中心为 O，某像素的单位射线方向为 r，则：

```
X(lambda) = O + lambda * r
```

改变 lambda 会改变真实 XYZ，但不会改变该点的图像投影。
中心线与运动先验可以缩小范围，却未必把范围缩为一个点。

脚本 `src/v13_depth_identifiability.py` 给出可复现的反例：

- 在第 200–300 帧，对 V12 轨迹沿各自观察射线施加平滑的 ±3 mm 深度变化；
  其他帧保持原样。
- 得到两条完整 XYZ 轨迹，最大相差 **6.00 mm**。
- 两条轨迹的最大投影差约 **1.27 × 10⁻¹³ px**，即数值精度内完全相同。
- 两条轨迹都在原半径代理约束内，最大 XYZ 速度均不超过 **60 mm/s**。
- 改动区间 101 帧中 92 帧有当前投影的实测宽度，9 帧位于 3 mm 下限（声明的假设，
  不是填补值）；反例不依赖任何填补值。

这并不证明其中任何一条是真实轨迹，而是证明：当前像素观测、相机假设、半径代理和
速度约束不足以在它们之间作出选择。更强的平滑先验可以偏好某条轨迹，
但这种偏好不是新的深度测量。

图：`out/figs/v13_depth_counterexample.png`。
两条完整轨迹及断言结果：`data/v13_depth_audit.npz`、`out/v13_depth_report.json`。

## 当前能可靠交付什么

1. 逐帧模型条件下的 XYZ 代表估计，并明确观测缺失、半径未知和局部约束不相容。
2. 在相机、弧长位置及球形半径代理固定的条件下，观察射线在模型内的近端/远端 XYZ。
   862 帧有这样的条件区间，区间长度中位数 8.28 mm，p90 10.71 mm（策略 C；旧版
   填补半径下为 733 帧、9.02 / 11.94 mm）。
   这不是标定过的置信区间，也不是真实管腔尺寸。
3. 给定条件下的速度和投影一致性检验。当前结果没有独立 3D 真值可用于精度验证。

## 进一步消除歧义需要的观测

最直接的是同一时刻的第二视角，以及两视角的相机标定。
对应像素的两条射线可以确定空间交点；在噪声下估计两射线的最近点并量化误差。

如果必须只用单视角：完整血管 STL、相机标定、机器人真实尺寸/形状或经过验证的
运动模型可以增加约束。它们有助于缩小允许的深度范围，但仅增加 STL 或更换滤波器
并不自动保证唯一位置。需要针对实际新增观测再次检验可辨识性。

运行：

```powershell
& 'C:\Python314\python.exe' src\v13_depth_identifiability.py
```
