# V12：防抖与机器人轨迹对齐

本轮解决视频中的可见追踪错位。观看 `out/figs/V12_stabilized_aligned.mp4`，
局部前后对照见 `out/figs/v12_before_after.jpg`。

## 原因

背景有轻微移动，但不能解释主要错位。使用机器人路线之外的背景特征，
测得相对首帧的画面中心位移中位数 1.26 px、p90 2.11 px、最大 3.31 px，
最大旋转约 0.18°。V11 输出与机器人原始检测的中位偏差约 10.7 px。

旧黄色曲线是 `Path 2.csv` 的血管中心线投影，本来就不是机器人贴壁后的实际轨迹。
此外，V11 的陷波处理削弱了可见运动，离散横向状态与强中心线先验会使显示点
离机器人较远。旧报告把输出与离线平滑趋势比较，无法充分描述这种屏幕上的错位。

## 修正

- 每帧使用固定背景特征估计相似变换，将画面与机器人检测坐标一起变换到首帧坐标系。
  特征区由已有的中心线投影排除机器人路线，不用未来机器人检测来选配准特征。
- 2D 跟踪保留机器人可见运动，只用因果自适应平滑减少微小跳动。
  黄线表示最近两秒的机器人 2D 轨迹，黄色圆圈表示当前检测的平滑位置。
  血管中心线仍以低对比虚线显示，两者没有被混为一条曲线。
- 3D 沿用 V11 延迟 12 帧的分支先验，在该分支内连续优化弧长。
  使用准确的相机射线求距离中心线最近的候选点，再施加半径和速度约束。
  黄线位置与 3D 投影之间的剩余差别明确显示。
- 完整 XYZ 位移与轴向位移均限速 60 mm/s；因遮挡失去观测时不伪造位置，
  恢复后重新初始化。上限验证不跨越缺失区间。
- 视频固定背景、3D 视角和放大区域，使用自然颜色。每一帧画面与叠加结果均对应
  同一采集时间。3D 分支信息仍有 0.40 s 延迟，视频是同步回放。

## 实测

以下比较使用两种方法共同接受的同一批 1007 帧，参考为相同的原始机器人检测。
这衡量图像一致性，不是独立的检测精度或真实 3D 精度验证。

| 指标 | V11 | V12 |
|---|---:|---:|
| 图像位置中位偏差 | 10.90 px | 黄色 2D 轨迹 1.90 px |
| 图像位置 p90 偏差 | 24.11 px | 黄色 2D 轨迹 2.94 px |
| 受约束的 3D 投影中位偏差 | 10.90 px | 2.04 px |
| 受约束的 3D 投影 p90 偏差 | 24.11 px | 5.44 px |
| 受约束的 3D 投影 >30 px | 3.48% | 1.89% |

（以上为半径策略 C 下的重跑结果：管径在当前相机投影上实测，未测处取 3 mm 下限，
不再用中位数填补——见 `report.md` V8b。旧版填补半径下的数字为 V12 列 2.03 / 4.58 /
1.59%；分叉处半径从填补的 5.3 mm 变为 3.0 mm 下限后，3D 投影 p90 与 >30 px 略升。）

配准另外留出四分之一特征，未用于拟合。留出特征的位移中位数从
1.40 px 降到 0.43 px。13 帧未通过配准质量门限，沿用上一有效变换。
有效 3D 相邻输出的最大轴向速度与最大 XYZ 总速度均为 60 mm/s，无超限。

## 尚未解决的模型冲突

70 帧需要的径向距离超过半径代理（当前投影实测宽度，未测处取 3 mm 下限；旧版用
L1 宽度加中位数填补时为 62 帧，冲突增加是因为分叉处半径从 5.3 mm 变为 3.0 mm）；
这只是**模型不相容**，不能据此认定机器人在真实血管外，更不能把该半径当成已测量的
3D 管腔表面。62 帧受到总速度限制，另有 18 帧检测丢失。

视频遇到半径/运动限制时用橙红色叉号显示受约束的 3D 投影，黄色圆圈继续表示
可见机器人；红线显示两者差距。检测丢失时不画机器人点。
因此，本轮已解决大部分显示与追踪错位，但没有宣称所有帧的 3D 位置都已被
给定解剖验证。真实深度仍取决于相机标定、管腔截面和单视角不可观测自由度。

## 文件与运行

- `src/v12_alignment_audit.py`：独立背景运动测量、V11 几何审计。
- `src/v12_aligned_tracking.py`：对齐、连续射线映射、约束、视频和 CSV。
- `src/test_v12_alignment.py`：已知相机运动恢复、精确重投影、半径边界、缺失观测测试。
- `data/v12_aligned.npz`：逐帧变换、2D 位置、3D 位置及状态。
- `out/v12_alignment_report.json`：机器可读指标。
- `out/v12_aligned_tracking.csv`：含采集时间、3D 可用时间及冲突状态的轨迹表。

在本机 PowerShell 中：

```powershell
& 'C:\Python314\python.exe' src\v12_alignment_audit.py
& 'C:\Python314\python.exe' src\test_v12_alignment.py
& 'C:\Python314\python.exe' src\v12_aligned_tracking.py --render
```

V12 使用既有相机、半径、检测缓存及 V11 分支先验。新增配准与细化约
5.0 ms/frame 中位数，不包括视频解码、既有检测和 V11 分支计算，
不应将该数当作从原始 fluoroscopy 图像开始的完整实时系统速度。
